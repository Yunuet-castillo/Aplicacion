
name: aplicacion
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.12.1
  firebase_auth: ^5.5.1
  path_provider: ^2.1.2
  cupertino_icons: ^1.0.8
  cloud_firestore: ^5.6.5
  google_fonts: ^6.2.1
  firebase_analytics: ^11.4.4
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  flutter_launcher_icons: ^0.14.1

flutter_icons:
  android: true
  ios: true
  image_path: "assest/logo_aplicacion.png"

flutter:
  uses-material-design: true

fonts:
  - family: ComicSans
    fonts:
      - asset: fonts/ComicSans.ttf
        

      
